services:
  orchestrator:
    build: .
    container_name: coduck-orchestrator
    ports:
      - "3000:3000"
    volumes:
      - ./orchestrator.sqlite:/app/orchestrator.sqlite
    environment:
      - ORCHESTRATOR_PORT=3000
    restart: unless-stopped
    command: node dist/orchestrator.js
