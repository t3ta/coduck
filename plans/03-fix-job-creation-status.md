# Task 03: Job作成時のstatus検証

## 優先度
🟡 中優先度（Codexレビュー指摘事項）

## 問題
`POST /jobs` がクライアントから任意の初期ステータスを受け入れている。オーケストレータ管理下のJobは一貫して `pending` から始まるべき。

## 目標
- Job作成時にstatusを強制的に `pending` に設定
- クライアントから送信されたstatusを無視

## 実装内容

### 1. routes/jobs.ts の POST /jobs ハンドラ修正

```typescript
router.post('/', (req, res, next) => {
  try {
    const body = createJobSchema.parse(req.body);

    // statusを強制的にpendingに設定（クライアントの入力を無視）
    const job = createJob({
      ...body,
      status: 'pending'  // 常にpendingから開始
    });

    res.status(201).json(job);
  } catch (err) {
    next(err);
  }
});
```

### 2. createJobSchema の調整
- statusフィールドをオプショナルにするか、スキーマから削除
- ドキュメントコメントで「statusは常にpendingから開始される」と明記

## 検証方法
1. status='running' を含むリクエストボディで `POST /jobs` を実行
2. 作成されたjobのstatusが `pending` になっていることを確認

## 関連ファイル
- `src/orchestrator/routes/jobs.ts`

## 備考
これにより、ジョブのライフサイクルが明確になり、オーケストレータの責任範囲が明確化されます。
